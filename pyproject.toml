[project]
name = "tariff-simulator"
version = "0.1.0"
description = "A simulator for calculating tariffs."
requires-python = ">=3.13"
dependencies = [
    "bblocks-data-importers==0.5.0",
    "bblocks-places>=0.0.4",
    "pandas>=2.3.3",
    "platformdirs>=4.10.0",
    "pydeflate>=2.5.0",
]

